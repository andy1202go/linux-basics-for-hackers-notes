# Linux 学习进度与评分跟踪

> 教材：《Linux Basics for Hackers》by OccupyTheWeb（以项目内 md 笔记为原文）
> 教学方式：启发式问答 | 格式：原文→翻译→深度解读→检查题
> 结构说明：**进度**与**打分**分开管理；每个 Module 总分 **10 分**，按题目难易加权；你的反问疑问单独记录为弱项。
> 学习者偏好：记忆需附**命令缩写对应的英文原词**（如 in-place 之类），对记忆更有效；每条命令讲解都附词根来源。

---

# 📌 第一部分 · 学习进度（学没学、学到哪）

## 进度总览

| 模块 | 标题 | 状态 | 完成日期 |
|---|---|---|---|
| 00 | 开始与搭建实验环境 | ✅ 已完成 | 2026-09-11 |
| 01 | 终端基础 | ✅ 已完成 | 2026-09-11 |
| 02 | 文本操作 | ✅ 已完成 | 2026-09-11 |
| 03 | 管理网络 | ✅ 已完成 | 2026-09-11 |
| 04 | 软件管理 | ✅ 已完成 | 2026-09-11 |
| 05 | 文件与目录权限 | ✅ 已完成 | 2026-09-11 |
| 06 | 进程管理 | ✅ 已完成 | 2026-09-11 |
| 07 | 环境变量 | ✅ 已完成 | 2026-09-14 |
| 08 | Bash 脚本 | ✅ 已完成 | 2026-09-14 |
| 09 | 归档与压缩 | ✅ 已完成 | 2026-09-15 |
| 10~17 | 其余模块 | ⬜ 未开始 | — |

## 当前学习位置

- 最近完成：Module 09 — 归档与压缩（8.4/10）
- 进行中：Module 10 — 文件系统与存储设备（未开始，下一步）
- 学习可断断续续，随时从此处续接。

---

# ⭐ 第二部分 · 打分（学得好不好）

> 规则：每章总分 **10 分**，拆到各道检查题，题目越难分值越高；答对得全分，部分正确/求助后按比例计分；同一章前后可微调权重，保证总和恒为 10。

## 评分总览

| 模块 | 总分 | 已得 | 状态 |
|---|---|---|---|
| 00 | 10.0 | 10.0 | ✅ 达标 |
| 01 | 10.0 | 10.0 | ✅ 达标 |
| 02 | 10.0 | 9.3 | ✅ 达标（错题1道：tail默认行数）|
| 03 | 10.0 | 9.7 | ✅ 达标（错题1道：dig+grep关键字）|
| 04 | 10.0 | 9.7 | ✅ 达标（错题1道：upgrade前须update）|
| 05 | 10.0 | 8.7 | ✅ 达标（错题3道：chmod语法/换算/SUID表述）|
| 06 | 10.0 | 9.0 | ✅ 达标（错题2道：fg/bg、Ctrl+Z）|
| 07 | 10.0 | 9.8 | ✅ 达标（错题1道：HISTSIZE拼写）|
| 08 | 10.0 | 8.4 | ✅ 达标（错题3道：比较辨析/引号展开/read顺序，已巩固）|
| 09 | 10.0 | 8.4 | ✅ 达标（错题3道：旗标z/j、tar -f顺序、dd层次/表述简略）|
| 10~17 | — | — | 未开始 |

---

# Module 00 — 开始与搭建实验环境

## 📌 学习进度

### 知识点清单（全部已过）

| 状态 | 知识点 | 要点 |
|---|---|---|
| ✅ | 为什么 Linux 对黑客友好 | 开源（源码公开可读改）+ 透明（系统运行状态全可见） |
| ✅ | 什么是发行版（distro） | 同内核 + 不同打包；Ubuntu/Fedora/Kali 都是 Linux |
| ✅ | Kali Linux 定位 | 面向安全，预装数百种工具；行业标准 |
| ✅ | Debian 地基 | Kali 基于 Debian 构建，软件包/命令兼容 |
| ✅ | 虚拟机 VM | 电脑里的电脑；host(真机)=宿主，guest=客户机 |
| ✅ | VM 隔离意义 | 破坏性操作留在 guest，重置即可恢复，host 不受影响 |
| ✅ | VirtualBox | Oracle 免费虚拟化软件，创建/运行 VM |
| ✅ | ISO 文件 | 磁盘映像=整张系统光盘的文件；VirtualBox 用它安装系统 |
| ✅ | BIOS 虚拟化 | VT-x(Intel)/AMD-V(AMD)，未开启是 VM 启动不了的最常见原因 |
| ✅ | RAM 分配规则 | host 每 4G 给 VM 1G；虚拟硬盘 ≥20G |
| ✅ | root 账户 | 管理员最高权限账户；安装时设密码 |

### 学习情况总结

- 完整通过 5 讲 + 5 道检查题。
- 强项：底层可见性理念、发行版概念、VM 隔离、VT-x 排查。
- 待巩固：安装流程完整步骤（用户名/密码只是其中一环，需装完整个系统）。

## ⭐ 打分

| 检查题 | 难度 | 分值 | 你的得分 | 备注 |
|---|---|---|---|---|
| 为什么黑客偏爱Linux | 中 | 2.0 | 2.0 | — |
| 关于Kali的四选一 | 中 | 2.0 | 2.0 | — |
| 虚拟机炸机处理 | 易 | 1.5 | 1.5 | — |
| ISO安装流程排序 | 易 | 1.5 | 1.5 | — |
| VT-x报错综合题 | 难 | 3.0 | 3.0 | 需补全细节后满分 |
| **合计** | | **10.0** | **10.0** | |

## ❓ 你的疑问与解答

- （本模块无反问记录）

---

# Module 01 — 终端基础

## 📌 学习进度

### 知识点清单（全部已过）

| 状态 | 知识点 | 要点 |
|---|---|---|
| ✅ | 终端是什么 | 文本对话式接收命令；黑客工具多为纯命令行无 GUI |
| ✅ | Linux 文件系统结构 | 倒挂树，根 `/`；一切尽在树内 |
| ✅ | 常见目录 | /etc 配置、/bin 程序、/home 普通用户、/root 管理员、/tmp 临时(重启清空) |
| ✅ | pwd | Print Working Directory，显示当前所在目录 |
| ✅ | ls | 列出目录内容；-l 详细、-a 隐藏文件(点开头) |
| ✅ | cd | 切换目录；..上级、~家目录、/根、-上一个目录 |
| ✅ | man | 查看命令手册；j/k 上下滚、/搜索、q 退出 |
| ✅ | whoami | 显示当前登录用户 |
| ✅ | locate / updatedb | 按名全文搜索文件；locate 基于预建库，新文件需 updatedb |
| ✅ | 大小写敏感性 | Linux 区分大小写；Desktop≠desktop≠DESKTOP |

### 学习情况总结

- 完成全部 8 讲知识点。
- 强项：导航命令、路径理解、大小写敏感。
- 弱项（已解答）：对"系统级操作 / root 权限"概念不熟，见下方疑问区。

## ⭐ 打分

| 检查题 | 难度 | 分值 | 你的得分 | 备注 |
|---|---|---|---|---|
| M01-Q1 终端多选 | 易 | 1.0 | 1.0 | — |
| M01-Q2 文件系统判断 | 中 | 1.5 | 1.5 | — |
| M01-Q3 目录映射 | 中 | 1.5 | 1.5 | — |
| M01-Q4 pwd/ls | 中 | 1.5 | 1.5 | Q3作答后被补全 |
| M01-Q5 cd | 易 | 1.0 | 1.0 | — |
| M01-Q6 man | 易 | 1.0 | 1.0 | — |
| M01-Q7 whoami大小写 | 中 | 1.5 | 1.5 | — |
| M01-Q8 locate | 易 | 1.0 | 1.0 | — |
| **合计** | | **10.0** | **10.0** | |

## ❓ 你的疑问与解答（弱项记录）

| 日期 | 你的疑问 | 解答 |
|---|---|---|
| 09-11 | 什么是"系统级操作"？为什么 `updatedb` 需要 root？ | 条件：①作用范围全局（重建覆盖全系统的搜索库）②改动系统区域（`/var/lib/` 等）③存在安全/稳定风险。普通用户默认无权改系统目录，需 `sudo` 临时提权；最小权限原则。 |
| 09-11 | `cd -` 的返回逻辑 | 跳到"上一个所在目录"（不是父目录），实现两个目录间快速往返。 |

---

# Module 02 — 文本操作

## 📌 学习进度

### 知识点清单

| 状态 | 知识点 | 要点 |
|---|---|---|
| ✅ | 文本工具的意义 | 万物皆文本；用工具过滤/分析大输出 |
| ✅ | cat | 读取整个文件；`>` 覆盖写、`>>` 追加写 |
| ✅ | grep | 按模式过滤行；-i忽略大小写、-r递归、-n行号、-v反选 |
| ✅ | head / tail | 头/尾**默认10行**；tail -f 实时跟踪日志 |
| ✅ | nl | 给输出加行号；常配合 grep/sed 引用定位 |
| ✅ | less | 分页阅读大文件；空格下一页、b上一页、/搜索、q退出 |
| ✅ | sed | 流式替换；s/旧/新/g；默认预览、-i 回写(先备份) |
| ✅ | 管道 \| | 前命令输出喂给后命令，可无限串联；区别 `>`（写文件）|

## ⭐ 打分

> 本章总分 10 分，按难度分配（将在每题通过后逐题计入）。

| 检查题 | 难度 | 分值 | 得分 |
|---|---|---|---|
| M02-Q1 文本意义+cat | 中 | 1.5 | 1.3（Q4表述不完整）|
| M02-Q2 grep | 难 | 2.0 | 2.0 |
| M02-Q3 head/tail/nl | 中 | 1.5 | 1.0（tail默认行数记错）|
| M02-Q4 less | 易 | 1.0 | 1.0 |
| M02-Q5 sed | 难 | 2.0 | 2.0 |
| M02-Q6 管道综合 | 难 | 2.0 | 2.0 |
| **合计** | | **10.0** | **9.3** |

## 📌 重点内容记录（你要求增加的）

- **cat 三态**：`cat file` 读文件；`cat > file` 覆盖写、`cat >> file` 追加写；`>` 会清空文件，`>>` 不会——**重定向符号才是幕后主角**。
- **grep 高频选项速记**：
  - `-i`（ignore，忽略大小写）——搜索时不用纠结大小写
  - `-n`（number，行号）——定位"这一条在文件第几行"
  - `-v`（invert，反转）——反选，看"不含"该词的行（如找日志里"除某个 IP 外还有谁"）
  - `-r`（recursive，递归）——钻进整个目录树搜，是"对整个目录做关键字侦查"的利器，比逐个 `cd` 子目录再 grep 快得多。
- **head/tail 易错点**：`head`/`tail` 默认都是 **10 行**（不是20）；要指定数量用 `-n`。
- **tail -f 使用场景**：实时跟踪日志，终端保持打开、新行即时打印。
- **数据提取心法**：大输出 → 用 head(头)/tail(尾)/grep(过滤)/less(翻页)，别用 cat 硬灌。
- **sed 定义与使用方式（完整）**：
  - **定义**：sed = **s**tream **ed**itor（流编辑器），逐行流式处理文本，最常用于**批量查找替换**。
  - **语法**：`sed 's/找/换成/g' 文件`
    - `s` = substitute（替换）；`找` = 被替换原文；`换成` = 新内容；`g` = global（每个出现都换，不加只换每行第一处）
  - **默认只预览**：不加 `-i` 时只把替换后的结果打印出来，**不改原文件**（安全）。
  - **`-i`** = in-place（就地修改，直接写回原文件）；**永久、无撤销，改前先 `cp 文件 文件.bak` 备份**。
  - **使用时机**：批量改配置文件、批量清洗输出。示例：`sed -i 's/mysql/MySQL/g' config.txt`。
- **sed 英文词根**：**sed**=stream editor；**`s`**=substitute；**`g`**=global；**`-i`**=in-place。

## ❓ 你的疑问与解答（弱项记录）

| 日期 | 你的疑问 | 解答 |
|---|---|---|
| 09-11 | sed 用法/缩写 | 见上方"重点内容记录-sed"。 |

## 📌 学习情况总结（Module 02）

- 全部 6 讲知识点已完成。
- 得分 **9.3 / 10**（扣分项：Q1 表述略欠、tail 默认行数记错，均已入错题本）。
- 强项：grep 选项、管道串联、sed -i 安全意识、命令英文词根记忆法。
- 弱项（已记录）：tail/head 默认行数。
- **接续位置**：Module 02 已完成 → 下一模块 Module 03（管理网络）未开始。

---

# Module 03 — 管理网络

## 📌 学习进度

### 知识点清单

| 状态 | 知识点 | 要点 |
|---|---|---|
| ✅ | IP vs MAC 地址 | IP=逻辑地址(可改，像街道地址)；MAC=物理硬件标识(12位十六进制,烧录) |
| ✅ | ifconfig | 读写网络接口；接口 eth0/wlan0/lo；inet=IP、ether=MAC |
| ✅ | iwconfig | 无线接口信息；ESSID、信号强度、速率 |
| ✅ | dhclient | 向 DHCP 请求自动IP；-r 释放当前IP；对有线/无线(wlan0)通用 |
| ✅ | MAC 地址伪造 | 软件级 spoof；ifconfig eth0 hw ether ...；重启还原 |
| ✅ | dig / DNS | DNS 查询；ANSWER SECTION；记录类型 a/aaaa/mx/ns/txt |

## ⭐ 打分

> 本章总分 10 分，按难度分配（将在每题通过后逐题计入）。

| 检查题 | 难度 | 分值 | 得分 |
|---|---|---|---|
| M03-Q1 IP/MAC+ifconfig | 中 | 1.5 | 1.5 |
| M03-Q2 iwconfig+dhclient | 中 | 1.5 | 1.5 |
| M03-Q3 MAC伪造+dig | 中 | 1.5 | 1.2（Q4 grep关键字不准）|
| M03-Q4 DNS综合收尾 | 难 | 3.0 | 3.0 |
| M03-Q5 网络概念综合 | 难 | 2.5 | 2.5 |
| **合计** | | **10.0** | **9.7 / 10** |

## 📌 重点内容记录（每个知识点：定义+英文全拼+核心要点）

### 知识点1：IP 与 MAC 地址
- **定义**：网络设备的两类身份标识。
  - **IP（Internet Protocol，互联网协议）**：逻辑地址，由路由器或手动分配，数据靠它路由"去哪"。类比**街道地址**，**可改变**。
  - **MAC（Media Access Control，媒体访问控制）**：物理硬件标识，制造商**烧录**在网卡里，**12 位十六进制**（如 `00:1A:2B:3C:4D:5E`），全球唯一。类比**身份证**，**不可永久改，只能临时伪造（spoof）**。
- **词根**：IP=Internet Protocol；MAC=Media Access Control。
- **核心要点**：IP 管"路由去哪、可改"；MAC 管"硬件身份、唯一、可临时spoof"。
- 记忆口诀：**IP=街道地址（逻辑、可改），MAC=身份证（物理、烧录、唯一）**。

### 知识点2：ifconfig
- **定义（全称）**：**ifconfig** = **i**nter**f**ace **config**uration（接口配置）——读写和管理网络接口的主命令。
- **词根**：**in**terface + **config**uration；**eth**er = 以太网；**wlan** = Wireless LAN。
- **常用接口**：
  - `eth0`：有线以太网（Ethernet）
  - `wlan0`：无线网卡（Wi-Fi / Wireless LAN）
  - `lo`：**loopback（回环）**，系统与自身通讯，**恒为 127.0.0.1**
- **关键输出字段**：`inet` = 当前 IP；`ether` = 当前 MAC。
- **常用命令**：
  - `ifconfig`：查看全部接口
  - `ifconfig eth0`：看单个接口
  - `ifconfig eth0 192.168.1.100`：改 IP
  - `ifconfig eth0 up / down`：启用/停用接口
- **核心要点**：看网络身份用 ifconfig；改 IP 后可能需要 down/up 重启接口生效。

### 知识点3：iwconfig
- **定义（全称）**：**iwconfig** = **i**nterface-**w**ireless **config**uration（无线接口配置）——无线版的 ifconfig，专门查看无线接口信息。
- **词根**：**i**nterface-Wireless + config；**ESSID** = Extended SSID（扩展服务集标识，即 Wi-Fi 网络名）。
- **展示信息**：ESSID（网络名）、信号强度、传输速率。
- **用法**：多数时候只**读取查看**，很少写入。
- **核心要点**：确认"连哪个 Wi-Fi、信号如何"用 iwconfig，与 ifconfig 互补（ifconfig 管有线+通用，iwconfig 管无线详情）。

### 知识点4：dhclient
- **定义（全称）**：**dhclient** = **DHCP client**（DHCP 协议的客户端）——向 DHCP 服务器请求/释放 IP。
- **词根**：**DHCP** = **D**ynamic **H**ost **C**onfiguration **P**rotocol（动态主机配置协议）；client = 客户端。
- **常用命令**：
  - `dhclient eth0`：请求自动 IP
  - `dhclient -r eth0`：释放（release）当前 IP，`-r` = release
- **核心要点**：手动 ifconfig 改死 IP 后上不了网 → `dhclient eth0` 回 DHCP 自动获取；`-r` 释放后再请求可换新 IP；**对有线(eth0)/无线(wlan0)都通用**。
- 易错点：手动设 IP ≠ 脱离 DHCP，需主动 dhclient 重新获取。

### 知识点5：MAC 地址伪造（spoof）
- **定义**：在软件层面临时改变网卡的 MAC 地址，伪装成别的硬件身份。
- **词根**：**spoof** = 伪造/仿冒；**hw ether** = **h**ard**w**are + **ether**net（硬件以太网地址，即 MAC）。
- **三步顺序**（先停→改→启）：
  ```bash
  ifconfig eth0 down                 # 1 停用接口
  ifconfig eth0 hw ether 00:11:22:33:44:55   # 2 设新 MAC
  ifconfig eth0 up                   # 3 启用接口
  ```
- **特性**：改动只存在内存里，**重启后还原**为真实硬件 MAC；如需持久可用 `macchanger` 等工具。
- **核心要点**：为何先 down？改硬件地址需接口停止；确认改动用 `ifconfig eth0` 看 `ether` 行。

### 知识点6：dig / DNS
- **定义（全称）**：**dig** = **D**omain **I**nformation **G**roper（域名信息探查器）——查询 DNS 记录的命令。**DNS** = **D**omain **N**ame **S**ystem（域名系统，互联网的"通讯录"，把域名翻译成 IP）。
- **词根**：answer = 答案（查询结果）；record = 记录；**mx** = **M**ail **e**xchange（邮件交换）。
- **基本用法**：`dig google.com` → `ANSWER SECTION` 显示解析出的 IP。
- **记录类型速记**：
  | 记录 | 全称/含义 | 用途 |
  |---|---|---|
  | `a` | IPv4 地址记录 | 查域名 IPv4 |
  | `aaaa` | IPv4 地址（4倍）| 查域名 IPv6 |
  | `mx` | Mail eXchange | 查邮件服务器（侦察）|
  | `ns` | Name Server | 查权威域名服务器 |
  | `txt` | Text 文本记录 | 常含验证数据 |
- **黑客意义**：1.DNS 查询暴露目标基础设施（邮件/子域/NS）→ 侦察；2.DNS 可被操控（投毒/欺骗，后续模块）。

## ❓ 你的疑问与解答（弱项记录）

| 日期 | 你的疑问 | 解答 |
|---|---|---|
| 09-11 | 无线 wlan 的 IP 释放/重分配也用 dhclient 吗？ | **用**。dhclient 不区分有线/无线，对 wlan0 同样适用：`dhclient -r wlan0` → `dhclient wlan0`。因为 DHCP 只管 IP 自动获取，与接口是有线还是无线无关；wlan0 也是标准的以太网类接口。

## 📌 学习情况总结（Module 03）

- 全部 6 讲知识点已完成。
- 得分 **9.7 / 10**（扣分项：Q3 的 grep 关键字用 google.com 不精准，已入错题本）。
- 强项：IP/MAC 概念区分、dhclient 有线无线通用、dig 记录类型、MAC 伪造三步序。
- 弱项（已记录）：dig 输出中更精准的 grep 过滤关键字（IN）。
- **接续位置**：Module 03 已完成 → 下一模块 Module 04（软件管理）未开始。

---

# Module 04 — 软件管理

## 📌 学习进度

### 知识点清单

| 状态 | 知识点 | 要点 |
|---|---|---|
| ✅ | 仓库/软件管理理念 | apt-get 管理；sources.list 存仓库源 |
| ✅ | apt-get update | 刷新可用包清单（不装不升）；装前先跑 |
| ✅ | apt-get install | 安装包，自动处理依赖；-y 跳过确认 |
| ✅ | apt-get remove / purge | 删包；purge 连配置一起删 |
| ✅ | apt-get upgrade | 更新所有已装包；先 update 再 upgrade |
| ✅ | apt-cache search | 按关键字搜仓库包 |
| ✅ | git clone | 从 GitHub 拉源码工具（clone→cd→README→安装）|
| ✅ | sources.list 安全 | 慎加第三方源；新版Kali在 sources.list.d/kali.sources |

## ⭐ 打分

> 本章总分 10 分，按难度分配（将在每题通过后逐题计入）。

| 检查题 | 难度 | 分值 | 得分 |
|---|---|---|---|
| M04-Q1 update/install/remove | 中 | 1.5 | 1.5 |
| M04-Q2 upgrade/search/clone | 中 | 1.5 | 1.2（Q4未强调先update）|
| M04-Q3 sources.list综合 | 难 | 3.5 | 3.5 |
| M04-Q4 软件管理综合 | 难 | 3.5 | 3.5 |
| **合计** | | **10.0** | **9.7 / 10** |

## 📌 重点内容记录（每个知识点：定义+英文全拼+核心要点）

### 知识点1：软件仓库与管理理念
- **定义**：Linux 通过**仓库（repository）**安装软件——由发行版维护的在线软件库。只需报包名，系统自动下载、安装并**解决依赖（dependency）**。
- **词根**：**apt** = **A**dvanced **P**ackaging **T**ool（高级打包工具）；**repository** = 仓库；**dependency** = 依赖；**package** = 软件包。
- **来源文件**：`/etc/apt/sources.list` 记录系统知晓的仓库源列表。
- **核心要点**：中央化可信源 + 自动装依赖，杜绝乱下 exe 的安全隐患；适配器 Debian 系（Kali/Ubuntu）。

### 知识点2：apt-get update
- **定义（全称）**：**apt-get update** = 刷新本地"仓库可用包"索引。
- **作用**：**不装不升**，仅同步"有什么货、最新版本号"。装前必跑，跳过可能装到**旧版本或报错**。
- **用法**：`apt-get update`
- **核心要点**：它是装机前的"装前必刷"习惯；类似更新商品目录。

### 知识点3：apt-get install
- **定义**：下载并安装软件包，**自动处理依赖**。
- **用法**：`apt-get install 包名`；`-y` = yes，跳过确认提示。
- **词根**：install = 安装；`-y` = yes（自动应答）。
- **核心要点**：依赖自动拉取；`-y` 在已知要装什么时省去确认。

### 知识点4：apt-get remove vs purge
- **定义**：卸载软件包。
- **对比**：
  - `apt-get remove 包名`：删程序，**保留配置文件**
  - `apt-get purge 包名`：删程序 + **连配置文件一起删**
- **词根**：remove = 移除；purge = 清除。
- **核心要点**：彻底卸载用 purge；只想除程序留配置用 remove。

### 知识点5：apt-get upgrade
- **定义**：把**所有已装包**升级到仓库中的最新版本。
- **词根**：upgrade = 升级。
- **关键顺序**：**先 `apt-get update` 刷新清单 → 再 `apt-get upgrade`**；不先 update 可能报错或拿不到最新版。
- **核心要点**：upgrade 真正动/升级软件，update 只刷清单，二者互补，先 update 后 upgrade。

### 知识点6：apt-cache search
- **定义（全称）**：按关键字在**本地仓库索引（cache）**中搜索匹配的包。
- **词根**：apt-cache = apt 的缓存/索引数据库；search = 搜索。
- **用法**：`apt-cache search 关键字`；关键字可匹配包名+描述；支持多词 `apt-cache search password crack`。
- **核心要点**：不记得确切包名 → search 定位 → install 安装。

### 知识点7：git clone
- **定义**：从 **GitHub** 把整个项目仓库**完整克隆到本地**。
- **词根**：**git** = 分布式版本控制工具；**clone** = 克隆（复制整个仓库副本）。
- **用法**：`git clone https://github.com/用户名/工具名` → 当前目录生成同名文件夹。
- **安装流程**：clone → `cd 工具` → 读 README → 按说明安装（常 `pip install -r requirements.txt` 或跑 Python 脚本）。
- **核心要点**：最前沿黑客工具常**只存在于 GitHub、不进仓库**，git clone 是拿到新工具的关键入口。

### 知识点8：sources.list 与安全隐患
- **定义**：`/etc/apt/sources.list` 记录系统从哪些**仓库源**拉取软件。（新版 Kali 使用 `sources.list.d/kali.sources`，概念相同、路径演进）
- **词根**：sources = 源/来源；list = 列表；sources.list.d = 分割式的源目录。
- **风险**：官方源被发行版**维护验证**可信；第三方/来源不明源**未验证**，可能藏恶意包或装到同名魔改软件。
- **安全处方**：默认**不随手改**；确需加第三方源须慎重判断可信度，优先官方仓库或正规 GitHub 仓库。
- **核心要点**：加错源 = 装到没打算装的东西的系统入口风险。

## ❓ 你的疑问与解答（弱项记录）

| 日期 | 你的疑问 | 解答 |
|---|---|---|
| 09-11 | Kali 里没 `sources.list`，只有 `sources.list.d/kali.sources`？ | 新版 Kali 用**分割式源**：APT 合并 `sources.list` + `sources.list.d/*.sources`。官方源在 `kali.sources`（DEB822 格式）。看/加源走 `sources.list.d/kali.sources` 即可。实战记录 ✅ |

## 📌 学习情况总结（Module 04）

- 全部 8 讲知识点已完成。
- 得分 **9.7 / 10**（扣分项：Q2 未强调 upgrade 前须先 update，已强化）。
- 强项：update/upgrade 分工、apt-cache search、git clone 流程、sources.list 安全意识。
- 弱项（已记录）：upgrade 前须 update 的顺序强调；已通过本模块补强。
- 实战亮点：发现新版 Kali 用 sources.list.d/kali.sources（已记录）。

---

# Module 05 — 文件与目录权限

## 📌 学习进度

### 知识点清单

| 状态 | 知识点 | 要点 |
|---|---|---|
| ✅ | 权限三组 | Owner/Group/Others |
| ✅ | 权限三位 | r读/w写/x执行；数值4/2/1 |
| ✅ | ls -l 权限块 | 首位文件类型，后9位 ugo |
| ✅ | chmod 数字法 | 如755=rwxr-xr-x |
| ✅ | chmod 符号法 | +x、u+x、g-w；u/g/o/a |
| ✅ | chown | 改属主/属组；-R 递归；需root |
| ✅ | SUID | 特殊位；以属主身份运行；提权重点 |
| ✅ | find 找 SUID | find / -perm -u=s -type f |

## ⭐ 打分

> 本章总分 10 分，按难度分配（将在每题通过后逐题计入）。

| 检查题 | 难度 | 分值 | 得分 |
|---|---|---|---|
| M05-Q1 权限基础 | 中 | 1.5 | 1.2（Q3命令语法错）|
| M05-Q2 chmod | 中 | 1.5 | 1.0（Q2数字串算错）|
| M05-Q3 chown | 中 | 1.5 | 1.5 |
| M05-Q4 SUID+find | 难 | 2.5 | 2.0（Q1未强调以属主身份、Q4未解释参数）|
| M05-Q5 提权综合 | 难 | 3.0 | 3.0 |
| **合计** | | **10.0** | **8.7 / 10** |

## 📌 重点内容记录（每个知识点：定义+英文全拼+核心要点）

### 知识点1：权限三组
- **定义**：Linux 对每个文件/目录区分三类访问者。
- **词根**：**Owner** = 属主（创建者）；**Group** = 属组（共享访问的组）；**Others** = 其他所有人。
- **核心要点**：任何访问请求先判定身份落在哪一组，再按该组权限放行。

### 知识点2：权限三位
- **定义**：每组可拥有三种权限位。
- **词根**：**r** = read（读）、**w** = write（写）、**x** = execute（执行）；数值 **4/2/1**（对应 r/ w/ x）。
- **核心要点**：能查看/能修改删除/能作为程序运行；`x` 缺失是脚本跑不动的主因。

### 知识点3：ls -l 权限块
- **定义**：`ls -l` 第一列展示文件的 10 位权限字符串。
- **解析**：`-rwxr-xr--` = `类型` + `属主(u)` + `属组(g)` + `其他(o)`，各三位 rwx。
- **首字符**：`-` 普通文件、`d` 目录、`l` 链接等。
- **核心要点**：工具拒绝运行 = 执行位未设；给权限用 chmod。

### 知识点4：chmod 数字法
- **定义（全称）**：**chmod** = **ch**ange **mod**e（更改模式）——设置谁对文件做什么。
- **数值**：r=4、w=2、x=1；三位数字分别对应 属主/属组/其他。
- **常用标准**：`755`=rwxr-xr-x（脚本，属主全权，组/其他 rx）；`644`=rw-r--r--（普通配置，属主 rw，组/其他 r）；`777`=人人全权（危险，几乎不用）。
- **核心要点**：口诀 4/2/1 相加；755/644 常用、777 危险。

### 知识点5：chmod 符号法
- **定义**：用字母精确增删权限。
- **结构**：`[u/g/o/a][+/=][r/w/x]`；无目标默认 a（所有人）。
- **词根**：u=user(属主)、g=group(属组)、o=others(其他)、a=all(全部)。
- **示例**：`chmod +x`（所有人加执行）、`chmod u+x`（仅属主）、`chmod g-w`（属组去写）。
- **核心要点**：符号法适合微调，数字法适合整体设定。

### 知识点6：chown
- **定义（全称）**：**chown** = **ch**ange **own**er（更改属主）——重新指派文件的属主/属组。
- **词根**：change + owner；`-R` = **R**ecursive（递归）。
- **两种写法**：
  - `chown 用户 文件`：只改属主
  - `chown 用户:组 文件`：属主**冒号**组，同时改（冒号前属主、后属组）
- **`-R` 递归**：`chown -R 用户 目录` → 目录及内部所有文件一起改。
- **权限要求**：**改他人文件属主需 root**（高危操作）。
- **核心要点**：chmod 管"权限位(谁能做啥)"，chown 管"归属权(属于谁)"，二者维度不同，勿混。

### 知识点7：SUID（Set User ID）
- **定义（全称）**：**SUID** = **S**et **U**ser **ID**——特殊的权限位，改变程序的运行身份。
- **机制**：普通程序以**运行者**权限运行；设了 SUID 的程序以**属主**权限运行（若属主是 root → 以 root 身份运行，即使执行者是普通用户）。
- **权限串**：在**属主执行位**显示为 `s`，如 `-rwsr-xr-x`（属主 x 位被 s 取代）。
- **合法示例**：`/usr/bin/passwd` 需写 root 所有的 `/etc/shadow`，故必须 SUID。
- **风险**：误设的 SUID 程序，或写得烂的 SUID 程序可被诱导做坏事 → **提权入口**。
- **核心要点**：一个 root 属主 + SUID 的二进制，普通用户一运行就是 root 权限——审查此类文件是发现提权路径的关键。

### 知识点8：find 查找 SUID
- **定义**：系统级搜索带 SUID 位的文件（提权侦察第一步）。
- **命令**：`find / -perm -u=s -type f 2>/dev/null`
- **拆解**：
  - `find /`：从根目录开始搜
  - `-perm -u=s`：权限含 SUID（u 位是 s）
  - `-type f`：只要普通文件（排除目录）
  - `2>/dev/null`：把错误输出丢弃，保持输出干净
- **词根**：perm = permission（按权限过滤）；type = 类型；f = file。
- **核心要点**：跑在全系统搜集 root SUID 二进制，逐个评估是否有漏洞/可提权。

## ❓ 你的疑问与解答（弱项记录）

- （暂无）

## 📌 学习情况总结（Module 05）

- 全部 8 个知识点已完成。
- 得分 **8.7 / 10**（扣分项：chmod 命令语法、数字法换算、SUID 表述细节）。
- 强项：权限三组/三位、chown、SUID 机制、find 找 SUID、提权原理。
- 弱项（已记录）：chmod 符号法语法（`chmod rwx`）、数字法换算（766）。均入错题本。

---

## 错题本（错题 + 你的回答 + 错误解析 + 正确答案）

> 格式：每条含「错题」「你的回答」「错误解析」「正确答案」四要素，便于重点复习。

### Module 00 · VT-x 报错综合题
- **错题**：VirtualBox 报"VT-x is disabled"如何修复？装好后还要做什么才能进登录界面？
- **你的回答**：去 BIOS 打开；设用户名和密码。
- **错误解析**：把"设用户名/密码"当成终点，忽略了必须完成**整个安装向导**并重启。
- **正确答案**：去 BIOS 开 VT-x(Intel)/AMD-V(AMD)；须完整装完系统并重启才到登录界面，用户名/密码只是其中一环。

### Module 02 · 文本意义+cat（Q4）
- **错题**：面对数千行扫描输出，该怎么做？
- **你的回答**：`cat log | grep` 或 `tail`／检索（方向对，表述略欠）。
- **错误解析**：理解了对，但没点破核心——"用文本工具过滤到关心的行"，而非人肉读全量。
- **正确答案**：不要人肉逐行读；用 `cat \| grep`（或 tail/less）+ `/` 检索等工具**过滤出关心的行**再分析。

### Module 02 · head/tail 默认行数
- **错题**：`head`/`tail` 默认显示多少行？
- **你的回答**：20 行。
- **错误解析**：把"默认 10 行"记成 20。
- **正确答案**：默认都是 **10 行**；要指定数量用 `-n`（如 `tail -n 20`）。

### Module 03 · dig+管道综合
- **错题**：只查看 `dig google.com` 结果里 ANSWER 的 IP 行，grep 哪个关键字？
- **你的回答**：grep `google.com`。
- **错误解析**：google.com 会出现在多处（不只 IP 行），不够精准；Address 记录行都带 `IN`（Internet 类标志）。
- **正确答案**：`dig google.com \| grep "IN"`，用 `IN` 这类标识性字段过滤更准确。

### Module 04 · upgrade 前须先 update
- **错题**：`apt-get upgrade` 可以先不跑 `apt-get update` 吗？
- **你的回答**：可以（可能无法升到最新）。
- **错误解析**：原书强调**必须先 update**；不 update 可能报错或拿不到新版，习惯上"应当先 update"而非"可以"。
- **正确答案**：先 `apt-get update` 刷新清单 → 再 `apt-get upgrade`，确保升到最新且不报错。

### Module 05 · 权限基础（Q3 命令）
- **错题**：给工具脚本加执行权限的命令？
- **你的回答**：`chmod rwx`。
- **错误解析**：符号法不用数字 rwx；`rwx` 是权限值不是命令参数。
- **正确答案**：符号法加执行位用 `chmod +x 文件`（给所有人加 x）。

### Module 05 · chmod 数字法换算（Q2）
- **错题**：属主 rwx、组/其他 rx 的权限数字？
- **你的回答**：`766`（`-rwxrw-rw-`）。
- **错误解析**：属组/其他多算了写权；正确的是属主 rwx=7、组/其他 rx=5。
- **正确答案**：`chmod 755` = `-rwxr-xr-x`。

### Module 05 · SUID 表述（Q4）
- **错题**：`s` 位含义及 find 参数解释。
- **你的回答**：答出 SUID、root；但未强调"以属主(root)身份运行"、未解释 find 各参数。
- **错误解析**：SUID 的精髓是**以属主权限运行**，这比"当前用户可以执行"更本质；find 参数需逐一说明。
- **正确答案**：`s`=SUID 位于属主执行位，程序**以属主(常 root)身份运行**；`find / -perm -u=s -type f 2>/dev/null`（从根搜/SUID位/仅文件/隐藏错误）。

### Module 06 · 后台/前后台（fg/bg 方向）
- **错题**：把一个后台进程拉回前台、指定第 2 号任务。
- **你的回答**：`bg`、`bg 2`。
- **错误解析**：把 fg/bg 用反了——`bg` 是送后台、`fg` 才是拉前台。
- **正确答案**：拉回前台用 **`fg`**，指定第 2 号任务用 `fg 2`；`bg` 是送后台。

### Module 06 · 挂起键（Ctrl+Z）
- **错题**：挂起前台进程的快捷键。
- **你的回答**：Ctrl+X。
- **错误解析**：Ctrl+X 是剪切/退出类；挂起是 Ctrl+Z（suspend）。
- **正确答案**：**Ctrl+Z** 挂起（暂停不终止）；配合 `bg`/`fg` 恢复。

### Module 07 · HISTSIZE 拼写
- **错题**：不留命令历史：`export ____=0`。
- **你的回答**：HSITSIZE=0。
- **错误解析**：HIS**T**SIZE 的 T 位置写错。
- **正确答案**：`export HISTSIZE=0`（HISTory + SIZE）。

### Module 08 · 脚本基础（引号/read/取值）
- **错题**：①取值用？②read 写法 ③双引号里 $ 是否展开。
- **你的回答**：①答 echo ②`target = read ...` ③误以为双引号里 $ 不生效。
- **错误解析**：取值要加 `$`（--）；read 应是命令在前变量在后；双引号**会展开**变量、单引号才不展开。
- **正确答案**：①取值 `$name` ②`read -p "提示:" target` ③`echo "Hi $USER"` → Hi kali；`'Hi $USER'` 才是字面。

### Module 08 · 私有脚本权限（Q5）
- **错题**：仅自己运行的脚本用 `chmod` 几？
- **你的回答**：70（你说 744）。
- **错误解析**：744 会让 group/其他可读；私有只需属主自己全权。
- **正确答案**：`chmod 700`（仅属主 rwx；组/其他无权限）。

### Module 09 · tar.gz 旗标字母（Q2/Q3）
- **错题**：一步打包+gzip 压缩/解压的 tar 旗标。
- **你的回答**：`cvgf`、`xvgf`（把 gzip 旗标写成 g）。
- **错误解析**：gzip 的 tar 旗标是 **`z`**（gz**i**p 取 z），不是 g；`-j` 才对应 bzip2。
- **正确答案**：创建 `tar -cvzf 名.tar.gz ...`、解包 `tar -xvzf 名.tar.gz`（z=gzip、j=bzip2）。

### Module 09 · tar 命令参数顺序（错题巩固·甲）
- **错题**：一条命令把 `a.txt b.txt` 打包并用 gzip 压缩成 `pack.tar.gz`。
- **你的回答**：`tar -cvzf a.txt b.txt pack.tar.gz`。
- **错误解析**：把归档名写在**最后**了——`-f` 要求**紧随其后**的正是归档文件名。按你写的命令，`a.txt` 会被当成归档名（生成一个叫 `a.txt` 的归档），`b.txt` 与 `pack.tar.gz` 反而成了被打包的对象。
- **正确答案**：`tar -cvzf pack.tar.gz a.txt b.txt`（`-f` 后紧跟归档名，待打包文件放最后）。

### Module 09 · dd 层次、进度与取证表述（Q3-Q2/Q4/Q5 + Q4-5）
- **错题**：① dd 工作在哪一层？② 为什么 `dd if=/dev/sdb of=/dev/sda` 危险？③ 取证为何不直接分析原盘？④ 做整盘镜像并显示进度的命令？
- **你的回答**：① "底层"；② 只答出正确写法与"检查是否写反"，未说明危险原因；③ "不破坏证据"；④ `dd if=/dev/sda of=prove.img`（**漏了进度参数**）。
- **错误解析**：方向都对但都不具体——① 未点出**裸磁盘层/块级（raw disk level / block level）**；② 未说明它**会用 sdb 的内容覆盖 sda**、且**无确认、无撤销**；③ 未说明**先做镜像副本、只分析镜像，原始证物全程不被触碰/篡改**（证据保全）；④ 题目明确要求"显示进度"，`dd` 默认**静默无输出**，必须加 `status=progress`。
- **正确答案**：① dd 在**块设备层**按块逐字节复制，不理会文件系统结构；② 会**覆盖真实磁盘**且无确认无撤销 → 正确写法 `dd if=/dev/sda of=/dev/sdb`，动手前**双查 if/of、涉真盘三查**；③ 先 `dd if=/dev/sda of=disk_image.img` 做镜像，**原始证据不被触碰/篡改**，块级镜像含已删文件/碎片/slack space（Autopsy、Sleuth Kit 可分析）；④ `dd if=/dev/sda of=prove.img status=progress`。

---

## 待巩固清单（跨模块汇总）

- [ ] Module 00：安装流程完整步骤（用户名密码只是其中一个环节）。
- [ ] Module 01：系统级操作 / root 权限概念（已记录解答，可回看）。
- [ ] Module 05：chmod 符号法语法、数字法换算。
- [x] Module 09：压缩旗标 gzip=`-z` / bzip2=`-j`（Q2 错点；错题巩固乙、丙已答对，已巩固）。
- [x] Module 09：tar **`-f` 后必须紧跟归档名**（错题巩固甲写反；Q4-1 已答对，复验通过）。
- [ ] 通用（Module 09 连犯 2 次）：作答要落到**具体参数 / 具体机制**，避免"底层""不破坏证据"这类笼统表述。

---

## 修订记录

- 2026-09-11：引入"进度与打分分离"结构；打分改为按题难易加权、每章总分10；新增"疑问与解答"弱项记录。
- 2026-09-11：开始 Module 02，建立知识点清单与打分表。
- 2026-09-11：完成 Module 02（9.3/10）；新增"重点内容记录"与"错题本"；记录学习者"记英文原词"偏好；补充 sed 定义/用法。
- 2026-09-11：完成 Module 03（9.7/10）；从本模块起每个知识点完整记录定义+英文全拼+核心要点；记录 wlan/dhclient 疑问。
- 2026-09-11：完成 Module 04（9.7/10）；记录新版 Kali sources.list.d/kali.sources 实战发现。
- 2026-09-11：重建进度文件，修正 Module 04/05 章节顺序错乱（05错误排在04之前，现以正确顺序重排）。
- 2026-09-11：完成 Module 05（8.7/10）；权限/提权核心（chmod/chown/SUID/find SUID）已掌握；错题本新增2条。
- 2026-09-11：完成 Module 06（9.0/10）；进程管理核心（ps/top/kill/后台/信号）已掌握；错题本新增2条。
- 2026-09-14：修正 Module 07/06 板块顺序（07误排06之前，现已改为06→07正确顺序）；完成 Module 07（9.8/10）。
- 2026-09-14：完成 Module 08（8.4/10）；新增 1.5 讲脚本语法格式体系；记录双引号/单引号/比较辨析等易错点；错题新增1条。
- 2026-09-15：自 Module 09 续学（起点：5/8 知识点、得分 4.0/10）；补记 M09 压缩旗标错点到待巩固清单。
- 2026-09-15：完成 M09 第 3 讲（dd 块级复制/风险/取证）+ Q3（2.5/3.0，累计 6.5/10）；补记知识点 6~8；错题新增 2 条；修正进度总览「06~17」为「10~17」、评分总览新增 09 行。
- 2026-09-15：**Module 09 完成（8.4/10）**；Q4 收尾综合 1.9/2.0（Q5 漏 `status=progress`、取证表述简略）；`-f` 位置复验通过；新增「表述笼统」跨模块待巩固项；错题本 dd 表述条升级为 Q3+Q4 合并记录。

---

# Module 06 — 进程管理

## 📌 学习进度

### 知识点清单

| 状态 | 知识点 | 要点 |
|---|---|---|
| ✅ | 进程与 PID | 进程=运行中的程序实例；PID 唯一标识 |
| ✅ | 前台/后台进程 | foreground 依附终端；background 后台静默 |
| ✅ | ps aux | 全进程快照；a/u/x 含义；关键列 USER/PID/CPU/MEM |
| ✅ | top | 实时视图；k杀进程、M/P排序、q退出 |
| ✅ | 后台运行 `&`、jobs、bg/fg | 后台启动；任务号；前后台切换 |
| ✅ | Ctrl+Z 挂起 | 暂停前台进程不终止 |
| ✅ | kill / kill -9 | 温和终止/强制SIGKILL；慎杀root进程 |
| ✅ | pgrep / pidof | 按名称查 PID |
| ✅ | 信号 SIGTERM/SIGKILL/SIGHUP | 15/9/1；信号含义 |
| ✅ | ps aux 侦察意义 | 杀防护/找服务/看其他用户进程 |

## ⭐ 打分

> 本章总分 10 分，按难度分配（将在每题通过后逐题计入）。

| 检查题 | 难度 | 分值 | 得分 |
|---|---|---|---|
| M06-Q1 进程+PID+ps aux | 中 | 1.5 | 1.5 |
| M06-Q2 top | 中 | 1.5 | 1.5 |
| M06-Q3 后台/前后台 | 中 | 1.5 | 1.0（Q3 fg/bg搞反）|
| M06-Q4 kill+信号+综合 | 难 | 2.5 | 2.0（Q4缺"为何杀前看COMMAND"）|
| M06-Q5 收尾综合 | 难 | 3.0 | 3.0 |
| **合计** | | **10.0** | **9.0 / 10** |

## 📌 重点内容记录

### 知识点1：进程与 PID
- **定义**：**进程** = 运行中的程序实例；**PID（Process ID）** = 进程号，系统给每个进程分配的唯一编号。
- **词根**：process = 进程；PID = Process ID；instance = 实例。
- **核心要点**：程序是静态代码，进程是运行实例；要停进程必须知道它的 PID。

### 知识点2：前台 / 后台进程
- **定义**：按与终端的关系分类。
  - **Foreground（前台）**：依附终端、可见运行，关终端即死。
  - **Background（后台）**：静默运行，终端可继续用，不依附终端。
- **词根**：foreground/background = 前台/后台。

### 知识点3：ps aux
- **定义**：查看系统进程（我爱用的完整版）。`ps` = process status。
- **参数**：
  - `a` = all users（所有用户进程）
  - `u` = 面向用户，显示属主
  - `x` = 含不依附终端的后台/守护进程
- **关键列**：`USER`（属主）、`PID`（进程号，用于杀）、`%CPU`/`%MEM`（资源占用）、`COMMAND`（具体程序）。
- **特性**：**静态快照**，同一时刻，跑完不更新。
- **聚焦技巧**：`ps aux | grep 关键字` 用管道过滤特定进程/用户。
- **核心要点**：ps aux = 全系统进程的"侦察工具"，先看全貌再按需 grep。

### 知识点4：top
- **定义**：**实时**进程视图，每隔几秒刷新，**默认按 CPU 使用率排序**（最耗资源在最顶）。
- **词根**：top = 顶部/最上面。
- **交互键**：`k` = kill（输 PID 杀进程）、`M` = 按内存排序、`P` = 按 CPU 排序（默认）、`q` = 退出。
- **用途**：电脑变慢 → 开 top 定位资源凶手。
- **vs ps**：ps 是静态快照，top 是持续实时。

> **补充：ps -ef（Unix风格）**：`-e`=every(全进程)、`-f`=full format(完整格式，含 PPID 父进程)。比 `ps aux` 多了亲子关系/完整命令行，排查看进程归属更好用。

### 知识点5：后台运行 `&`、jobs、bg/fg
- **`命令 &`**：命令**一开始就后台运行**，终端立即归还。输出 `[任务号] PID`。
- **`jobs`**：列出当前会话所有后台任务及编号。
- **`bg`** = background：把**被挂起**的进程送到**后台**续跑。
- **`fg`** = foreground：把**后台**进程**拉回前台**接管终端；`fg 2` 指定第2号任务。
- **词根**：bg=background、fg=foreground、jobs=任务/作业。
- **核心要点**：`bg` 送后台、`fg` 拉前台，方向别反！多任务用编号 `fg N`/`bg N`。

### 知识点6：Ctrl+Z 挂起
- **定义**：挂起（暂停）正在前台运行的进程，**不终止**它。
- **词根**：suspend = 挂起/暂停。
- **配合**：挂起后 → `bg` 后台续跑 或 `fg` 回前台继续。
- **核心要点**：Ctrl+Z ≠ 杀进程，只是暂停，随时可恢复。

### 知识点7：kill 与 kill -9
- **定义**：`kill PID` 终止进程（本质是发信号）。
- **区别**：
  - `kill PID` = **SIGTERM(15)**，礼貌请求进程自行关闭。
  - `kill -9 PID` = **SIGKILL(9)**，强制立即终止，进程**无法忽略/拦截/延迟**。
- **比喻**：kill=请人离开；kill -9=物理架走。
- **安全警告**：**杀 root 系统进程前先确认**！用 `ps aux` 看 COMMAND，不懂就杀可能崩溃系统。谨慎性 = 渗透中暂停防护软件的技巧，风险自负。
- **核心要点**：先分清是普通程序还是 root 关键进程，再决定 kill 还是 kill -9。

### 知识点8：pgrep / pidof
- **定义**：已知进程名，直接取其 PID。
- **词根**：pgrep = process **grep**；pidof = PID **of**。
- **用法**：`pgrep 名`、`pidof 名` → 直接返回 PID，免翻 ps aux。
- **核心要点**：杀进程 → 已知名就用 pgrep/pidof 取 PID → kill。

### 知识点9：信号（SIGTERM/SIGKILL/SIGHUP）
- **定义**：`kill` 其实是在给进程发送**信号**。
- **速记**：
  | 信号 | 编号 | 作用 |
  |---|---|---|
  | SIGTERM | 15 | 礼貌关闭（默认）|
  | SIGKILL | 9 | 强制立即终止 |
  | SIGHUP | 1 | 重载配置（服务场景）|
- **词根**：SIG=SIGnal；TERM=terminate；KILL=强杀；HUP=hung process。
- **核心要点**：日常主要用 15(礼貌) 和 9(强杀)，其他信号在服务/守护进程场景出现。

### 知识点10：ps aux 的侦察意义
- **定义**：落地目标后，`ps aux` 查看系统正在跑什么，是**侦察**工具。
- **关注点**：① 杀毒/EDR 防护——知道了再决定是否暂停；② 有价值服务（数据库/Web/内网工具）——指明目标；③ 其他用户进程——情报。
- **核心要点**：ps aux 既是管理工具也是情报/侦察工具。

## ❓ 你的疑问与解答（弱项记录）

| 日期 | 你的疑问 | 解答 |
|---|---|---|
| 09-11 | ps 怎么看特定用户进程？能否只列守护进程？ | 特定用户/进程用管道最灵活：`ps aux \| grep root`、`ps aux \| grep 名`。ps 无"只列守护进程"开关；守护进程特征=无终端(TTY 为 `?`)，可用 `ps aux \| awk '{ if ($7=="?") print }'` 近似筛选，日常 grep 够用。 |
| 09-11 | `ps -ef` 中 -ef 含义？ | `-e`=every(全进程)、`-f`=full format(完整格式，含 PPID)。属 Unix/SysV 风格(带`-`)；`ps aux` 是 BSD 风格(无`-`)。`-ef` 长于看父子关系和完整命令行，`aux` 长于看资源占用。 |

## 📌 学习情况总结（Module 06）

- 全部 10 个知识点已完成。
- 得分 **9.0 / 10**（扣分项：fg/bg 方向、Ctrl+Z 挂起键、杀进程安全确认）。
- 强项：PID 概念、ps aux/top、kill 信号、后台 Job 概念、ps aux 侦察意义。
- 弱项（已记录）：fg 拉前/bg 送后的方向、Ctrl+Z vs Ctrl+X。均入错题本。

---

# Module 07 — 环境变量

## 📌 学习进度

### 知识点清单

| 状态 | 知识点 | 要点 |
|---|---|---|
| ✅ | 环境变量概念 | 系统内存中的命名值；大写命名；`$` 引用 |
| ✅ | 常用变量 | PATH/HOME/USER/SHELL/HISTSIZE/PS1 |
| ✅ | PATH 原理 | 冒号分隔目录列表；决定命令查找位置 |
| ✅ | env / echo | env 列全部；echo $VAR 查单个 |
| ✅ | 创建/修改变量 | MYVAR=... 会话内；export 传给子进程 |
| ✅ | 追加 PATH 避免覆盖 | export PATH=$PATH:/new |
| ✅ | 持久化 ~/.bashrc | export 只当前会话；写入 .bashrc + source |
| ✅ | PS1 / HISTSIZE 黑客用途 | 改提示符；HISTSIZE=0 不留痕迹 |

## ⭐ 打分

> 本章总分 10 分，按难度分配（将在每题通过后逐题计入）。

| 检查题 | 难度 | 分值 | 得分 |
|---|---|---|---|
| M07-Q1 概念+PATH | 中 | 2.0 | 2.0 |
| M07-Q2 env/export/追加PATH | 中 | 2.5 | 2.5 |
| M07-Q3 持久化+PS1/HISTSIZE | 中 | 2.5 | 2.5 |
| M07-Q4 收尾综合 | 难 | 3.0 | 2.8（Q5拼笔误 HSITSIZE→HISTSIZE）|
| **合计** | | **10.0** | **9.8 / 10** |

## 📌 重点内容记录

### 知识点1：环境变量概念
- **定义**：系统登录期间保存在**内存**中的命名值（键值对）；程序和 shell 据此决定行为。
- **命名与引用**：通常**全大写**；引用时前缀 **`$`**（如 `$HOME`）。
- **词根**：environment = 环境；variable = 变量；reference = 引用。
- **核心要点**：`echo $HOME` 会展开成实际值打印。

### 知识点2：常用环境变量
- **六大连表**：
  - `$PATH`：命令搜索的目录列表
  - `$HOME`：家目录路径
  - `$USER`：当前用户名
  - `$SHELL`：正在运行的 shell
  - `$HISTSIZE`：历史记住的命令条数
  - `$PS1`：命令提示符外观
- **词根**：PS1 = **P**rompt **S**tring **1**（提示符串1）。
- **核心要点**：记得这些变量是"系统在内存里存的设置"，可随时查改。

### 知识点3：PATH
- **定义**：用**冒号 `:`** 分隔的目录路径列表；敲命令时系统按顺序搜索每个目录找同名程序。
- **常见报错**："command not found" = 程序所在目录不在 PATH 里。
- **安全红线**：**弄坏 PATH = 几乎跑不了任何命令**（连 ls/cat 都找不到）→ 改 PATH 须谨慎。
- **词根**：PATH = 路径。
- **核心要点**：PATH 决定"你敲命令时系统去哪找";破坏它风险极大。

### 知识点4：env / echo / 创建变量 / export
- **env**：打印当前会话**所有**环境变量；`echo $VAR` 查单个。
- **创建**：`MYVAR="hello"` 仅当前 shell 会话，关终端消失。
- **export** = 导出：`export MYVAR="hello"` 使子进程（由此终端启动的程序）也能读取。子进程默认继承父环境，export 就是把它加入环境。
- **追加 PATH（易错）**：`export PATH=$PATH:新目录`——开头的 `$PATH` 保留原值再追加；漏写 `$PATH` 会抹掉其他目录、弄坏 PATH。
- **词根**：env=environment；export=导出。
- **核心要点**：env 看全部、echo 看单个；赋值只本会话、export 传子进程；加 PATH 必须 `$PATH:新目录`。

### 知识点5：持久化（~/.bashrc + source）
- **定义**：让变量跨会话、跨重启**永久生效**。
- **`.bashrc`**：bash 的**用户级配置文件**，每次开交互式 bash 自动加载；`~` = 家目录。
- **词根**：.bashrc = bash **r**un **c**ommands（bash 运行命令）；source = 来源/加载。
- **方法**：把 `export ...` 加进 `~/.bashrc` 底部 → 新终端自动生效；或 `source ~/.bashrc` **重新加载**立即生效（无需重启）。
- **核心要点**：source ≠ 重启 bash，只是重读配置文件；加完配置最稳妥是 source 或开新终端。

### 知识点6：PS1 / HISTSIZE（黑客用途）
- **PS1（Prompt String 1）**：控制命令提示符外观；`export PS1="自定义"` 立即改。
- **HISTSIZE(history size)**：历史命令保存条数。
- **OpSec 技巧**：`export HISTSIZE=0` 使**本会话不记录命令历史**→ 打开历史文件也看不到你跑过什么，是**上机不留痕迹**的基础操作。
- **词根**：HIST=history、SIZE=大小。
- **核心要点**：HISTSIZE=0 只对当前会话生效，是渗透/隐蔽的常规动作。

## ❓ 你的疑问与解答（弱项记录）

- （暂无）

## 📌 学习情况总结（Module 07）

- 全部 8 个知识点已完成。
- 得分 **9.8 / 10**（扣分项：Q5 拼写 HISTSIZE 笔误）。
- 强项：环境变量概念、PATH 追加、export 语义、持久化 source、PS1/HISTSIZE 黑客用途。
- 弱项（已记录）：HISTSIZE 拼写。已入错题本。

---

# Module 08 — Bash 脚本

## 📌 学习进度

### 知识点清单

| 状态 | 知识点 | 要点 |
|---|---|---|
| ✅ | 脚本概念 | 文本文件存命令列表；依次执行 |
| ✅ | shebang | #!/bin/bash 首行；指定解释器 |
| ✅ | 运行脚本 | chmod 755 + ./script.sh；$ 变量 |
| ✅ | echo / 变量 / read | 打印；赋值无$、用$取；read获取输入、-p提示 |
| ✅ | 注释 | # 开头；给人看的说明 |
| ✅ | if/else + 比较符 | fi收尾；-eq/-ne/-gt/-lt/-ge/-le；字符串用= / != |
| ✅ | for 循环 | for i in ... do/done；循环列表 |
| ✅ | 脚本结构 | 变量→输入→主逻辑→收尾；chmod 700 私有 |

## ⭐ 打分

> 本章总分 10 分，按难度分配（将在每题通过后逐题计入）。

| 检查题 | 难度 | 分值 | 得分 |
|---|---|---|---|
| M08-Q1 脚本基础 | 中 | 2.0 | 1.2（原0.9等比；Q3取值/ Q4顺序/ Q5双引号展开错）|
| M08-Q2 if/for/注释 | 中 | 4.0 | 4.0 |
| M08-Q3 收尾综合 | 难 | 4.0 | 3.2（原2.0等比；Q5改700写成744）|
| **合计** | | **10.0** | **8.4 / 10** |

## 📌 重点内容记录

### 知识点1：脚本概念
- **定义**：存命令列表的纯文本文件；运行时 Bash 逐行顺序执行，等同一个个敲命令，但**自动化、可复用**。
- **词根**：script = 脚本。
- **核心要点**：任何终端命令都能进脚本，再结合逻辑（if/循环）变成工具。

### 知识点2：shebang 行
- **定义**：`#!/bin/bash`，写在脚本**第一行**（前面无空行）。
- **词根**：shebang/hashbang = `#!` 两字符；`!` 读作 bang。
- **作用**：告诉系统用**哪个解释器**跑这个文件。
- **核心要点**：写错/漏掉可能用错 shell 导致诡异错误；永远第一行。

### 知识点3：运行脚本
- **三步**：①写（`#!` 开头）②`chmod 755 script.sh`（加执行权）③`./script.sh`（运行）。
- **`./` 含义**：从当前目录运行（当前目录通常不在 PATH）。
- **chmod 755**：属主 rwx、组/其他 rx；私有脚本可用 `chmod 700`。
- **核心要点**：Permission denied = 缺执行位，用 chmod 补。

### 知识点4：echo / 变量 / read
- **echo**：打印文本；`echo "..."`，可展开变量 `echo "$USER"`。
- **变量**：**赋值不加 `$`**（`name="Bob"`，`=` 两侧**无空格**）；**取值加 `$`**（`$name`）。
- **read**：脚本暂停等用户输入存变量；`read -p "提示:" var` 带行内提示。
- **引号与展开**：**双引号** `"$VAR"` 会展开变量；**单引号** `'$VAR'` 不展开（显示字面 $VAR）。
- **核心要点**：赋值 `=`两侧无空格、取值用 `$`；双引号展开、单引号不展开。

### 知识点4.5（1.5讲扩展）：Bash 脚本语法格式体系
> 定位：全章最易踩坑、最难排查的部分。大多数 bash 报错不是命令错，而是**格式不合规**。

**一、空格——最关键的判官**
- 赋值 `=` 两侧**严禁空格**：`name="Bob"` ✓ / `name = "Bob"` ✗（name 被当命令）
- 条件 `[ ]` 内侧**必须空格**：`[ $x -gt 10 ]` ✓ / `[$x -gt 10]` ✗
- `; do` / `; then` 前要有分号或换行

**二、引号分工**
| 写法 | 作用 | 变量展开 |
|---|---|---|
| `""` 双引号 | 当一个整体，保留含义 | ✅ 展开 |
| `''` 单引号 | 纯字面，字符全失去意义 | ❌ 不展开 |
| `` ` `` / `$()` | 命令替换：执行并取输出 | — |

**三、结束符（成对闭合）**
- if→**fi**、for/while→**done**、case→**esac**
- 口诀：结尾词是开头词**字母倒写**（fi、done、esac）

**四、`;` 分号 = 一行里的"换行"**：`if [ 条件 ]; then`、`for i in ...; do`

**五、变量与 `$`**：赋值无 `$`、取值加 `$`；数组 `${arr[0]}`；紧邻字符用 `${name}file`

**六、注释与 shebang**：`#` 到行尾是注释；第一行 `#!` 是 shebang（系统专用）

**七、常见报错速查**
| 报错 | 多半原因 |
|---|---|
| command not found | 赋值/条件多了空格、`[$x]` |
| syntax error near token | 漏 fi/done/esac 或 `; then` 格式错 |
| Permission denied | 没 chmod +x |
| 变量输出为空/错 | 赋值忘 `=` 或用错引号 |

**八、格式正确完整示例**：见课堂（#!→read→if/else→fi→for→done）

> 总口诀：`=`两侧莫留空、`[ ]`内侧要留空、块结尾`fi/done/esac`、双引展开单引字面、`read`命令在前变量在尾。

### 知识点5：if/else 与比较符
- **语法**：`if [ 条件 ]; then ... else ... fi`；`[ ]`内侧留空格、`fi`收尾（if倒写）。
- **数字比较**：`-eq`等于、`-ne`不等于、`-gt`大于、`-lt`小于、`-ge`≥、`-le`≤。
- **字符串比较**：用 `=`（等于）/ `!=`（不等）。
- **词根**：-eq=equal、-ne=not equal、-gt=greater than、-lt=less than、ge/le=greater/less-equal。
- **核心要点**：数字用 -xx 系列、字符串用 = / !=；别混。

### 知识点6：for 循环
- **语法**：`for 变量 in 列表; do ... done`；`done` 收尾。
- **取值**：`$i` 取当前值。
- **用途**：批量操作（循环 ping = 原始扫描器，同 nmap 思路）。
- **核心要点**：分号 `;` 在 do 前是"换行"；结束后必须有 done。

### 知识点7：注释与脚本结构
- **注释**：行首 `#` 整行被忽略；区别 shebang `#!`（首行系统用）。
- **脚本推荐结构**：`#!/bin/bash` → 注释(用途/作者/日期) → 变量 → 输入 → 主逻辑 → 收尾；分区块便于阅读。
- **核心要点**：注释写给未来的自己；结构清晰让 >20 行脚本也易懂。

### 知识点8：脚本综合纠错要点（收尾）
- **运行链路**：`#!` → 变量 → 输入(read) → 逻辑 if/for → 收尾(fi/done) → echo 输出。
- **易漏**：`for i in 列表`（漏 in）；循环/判断结束符（done/fi）。
- **私有脚本权限**：仅自己用 `chmod 700`（只属主 rwx）；需组/其他只读执行再考虑 755。
- **核心要点**：排查脚本 = 按格式体系逐条对；权限按"谁需要什么"设，尽量最小。

## ❓ 你的疑问与解答（弱项记录）

- （暂无）

## 📌 学习情况总结（Module 08）

- 全部 8 个知识点已完成（含 1.5 讲语法格式体系扩展）。
- 得分 **8.4 / 10**（扣分项：字符串数字比较辨析、双引号展开、read 顺序——均入错题本并已巩固）。
- 强项：shebang、运行脚本/权限、if/for 语法、结束符 fi/done、格式自检意识。
- 弱项（已记录）：字符串 vs 数字比较、双引号 vs 单引号展开、私有脚本 chmod 700。
- 新增巩固点：1.5 讲完整记录到知识点 4.5（脚本语法格式体系）。

---

# Module 09 — 归档与压缩

## 📌 学习进度

### 知识点清单

| 状态 | 知识点 | 要点 |
|---|---|---|
| ✅ | 归档 vs 压缩 | 归档=合并多文件（不减小）；压缩=编码变小的单文件 |
| ✅ | tar 三操作 | -c 创建 / -x 解包 / -t 列出；-v 显示 / -f 文件名 |
| ✅ | gzip / gunzip | 原地压缩单文件→.gz；gunzip 或 -d 解压 |
| ✅ | tar.gz 一步到位 | -z 联合 gzip；tarball |
| ✅ | bzip2 | 更高压缩比更慢；.bz2；tar 用 -j |
| ✅ | dd | 裸磁盘层（raw disk level）按块逐字节复制；if=input file（源）/ of=output file（目标）|
| ✅ | dd 风险 | 写反 if/of 或目标写错即毁盘；无确认、无撤销；跑前双查、涉真盘三查；status=progress 看进度 |
| ✅ | 取证用途 | dd 做整盘镜像→只分析镜像、原始证据不碰；捕获已删文件/碎片/slack space |

## ⭐ 打分

> 本章总分 10 分，权重已预设（合计恒为 10），每题通过后逐题计入得分。

| 检查题 | 难度 | 分值 | 得分 |
|---|---|---|---|
| M09-Q1 归档概念+tar | 中 | 2.5 | 2.5 |
| M09-Q2 gzip/bzip2/tar.gz | 中 | 2.5 | 1.5（旗标写成g应为z；已巩固）|
| M09-Q3 dd+风险 | 难 | 3.0 | 2.5（Q2层次未点"块级"、Q4缺危险原因、Q5表述简略）|
| M09-Q4 收尾综合 | 难 | 2.0 | 1.9（Q5 漏 status=progress；取证表述简略）|
| **合计** | | **10.0** | **8.4 / 10** |

## 📌 重点内容记录

### 知识点1：归档 vs 压缩
- **定义**：
  - **归档（archive）**：多个文件**合并为一个文件**，体积**不变小**（只捆绑）。工具 `tar`。
  - **压缩（compress）**：更高效编码使文件**变小**，只针对**单个文件**。工具 `gzip`/`bzip2`。
- **组合**：实践中**先归档再压缩**（因 gzip 只压单文件）→ 得到 `.tar.gz`。
- **词根**：archive=归档；compress=压缩；bundle=捆绑。
- **核心要点**：归档≠压缩；`.tar.gz` 后缀读法：`.tar`=归档过、`.gz`=gzip 压过（俗称 tarball）。

### 知识点2：tar 三操作
- **定义（全称）**：**tar** = **Ta**pe **Ar**chive（磁带归档），名字来自磁带备份时代。
- **7 旗标**：`-c` create创建 / `-x` eXtract解包 / `-t` lisT列出 / `-v` verbose详细 / `-f` file文件名(紧跟归档名) / `-z` gzip / `-j` bzip2。
- **三动作**：
  - 创建：`tar -cvf 名.tar 文件们`
  - 解包：`tar -xvf 名.tar`
  - 偷看（不解包）：`tar -tvf 名.tar`
- **核心要点**：共同点 `-vf`（v 显示、f 接文件名，f 必须紧跟文件名放最后）；变的只是动作字母 c/x/t。

### 知识点3：gzip / gunzip
- **定义（全称）**：**gzip** = **G**NU **zip**；**gunzip** = g**un**zip（un- 反义）。
- **原地替换**：`gzip 文件` → 原文件**消失**，变 `文件.gz`（破坏性，非复制）。
- **解压**：`gunzip 文件.gz` 或等价 `gzip -d 文件.gz`（`-d`=decompress）。
- **核心要点**：gzip 只压单文件、压完原文件没了；要压多文件先 tar 再 gzip。

### 知识点4：tar.gz 一步到位（-z）
- **定义**：`-z` 让 tar 顺手做 gzip，免去两步。
- **命令**：创建 `tar -cvzf 名.tar.gz 文件们`；解包 `tar -xvzf 名.tar.gz`。
- **词根**：`z` = gz**i**p（z 代表 gzip 层）；tarball = tar+ball，指 .tar.gz。
- **核心要点**：**旗标是 `z` 不是 `g`**（易错）；f 仍紧跟归档名。

### 知识点5：bzip2（-j）
- **定义**：另一种压缩工具，比 gzip **压得更小**但**更慢**。
- **对比**：gzip→`.gz`/旗标`-z`/快；bzip2→`.bz2`/旗标`-j`/慢。
- **命令**：`tar -cvjf 名.tar.bz2 文件们`；解包 `tar -xvjf 名.tar.bz2`。
- **词根**：bzip2 = block zip 2；`j` 对应 bzip2。
- **核心要点**：与 .tar.gz 唯一区别是 `-z`→`-j`。

### 知识点6：dd（块级复制）
- **定义（全称）**：`dd` 传统解释为 **d**ata **d**uplicator（数据复制器），也被认为源自 IBM JCL 的 **DD = Data Definition**（`if=/of=` 语法由此继承）。它在**裸磁盘层（raw disk level）**工作，**按块逐字节**复制，不理会文件系统结构。
- **词根/英文全称**：**if** = **i**nput **f**ile（源）、**of** = **o**utput **f**ile（目标）；`/dev/` = **dev**ices（设备目录，Linux「一切皆文件」，磁盘也是文件）；`sda` = **s**torage **d**evice **a**（第一块盘）；**raw** = 原始的；**block** = 块。
- **命令**：
  - 盘对盘克隆：`dd if=/dev/sda of=/dev/sdb`
  - 生成镜像文件：`dd if=/dev/sda of=disk_image.img`
  - 带进度：`dd if=/dev/sda of=disk_image.img status=progress`
- **核心要点**：**文件层工具（tar/gzip）vs 块设备层工具（dd）**——dd 不认识"文件"，只认字节，所以复制"完美"（含已删除文件、元数据、残留空间）。
- **易错点**：`if`/`of` 写反不是小错，是毁盘；`/dev/sda` 是**真实磁盘设备**，不是普通文件。

### 知识点7：dd 的风险 —— "Data Destroyer"
- **定义**：`dd` 一旦启动就以 `of` 指向的对象为**牺牲品**，直接覆写。写反 `if`/`of` 或目标写错，就会用零或垃圾**覆盖一块真实磁盘**。
- **词根**：**Data Destroyer** = 数据毁灭者（社区绰号）；**overwrite** = 覆写；**no undo** = 无可撤销。
- **危险示例**：`dd if=/dev/sdb of=/dev/sda` ← 用 sdb 的内容**覆盖你的主硬盘**。
- **防护规则**：跑前**双查 `if`/`of`**；**其中一个是真实磁盘就三查**。
- **易错点**：以为"复制命令不会破坏东西"——`dd` **不确认、不提示、无回收站、无撤销**；默认还**静默无进度**，所以要加 `status=progress` 才知道跑到哪。
- **实战意义**：这是"破坏性操作"的典型——渗透/运维里任何 `dd`、`of=`、`/dev/` 相关命令都要先停三秒确认目标。

### 知识点8：dd 的取证用途
- **定义**：数字取证（digital forensics）标准流程——扣押设备后**先用 `dd` 制作镜像，再分析镜像**，原始证物**全程不被触碰或改动**。
- **词根**：**forensics** = 取证/法证；**image** = 镜像（整盘逐字节副本）；**evidence** = 证据；**slack space** = 残留空间（文件占用最后一个块中未用满的部分，常藏旧数据）。
- **关键因果链**：块级复制 → 捕获**文件 + 已删除文件 + 文件碎片 + 文件系统结构 + slack space** → 用 **Autopsy / Sleuth Kit** 分析镜像 → 恢复**普通文件浏览器根本看不到**的数据。
- **与 `cp` 的对比**：`cp` 走文件系统，只复制"文件系统认为存在"的文件，**已删文件与残留空间都拿不到**；`dd` 绕过文件系统直接抄扇区，所以才能保全证据。
- **核心要点**：**证据保全** = 只分析副本、原始不动（保证可追溯、可用于法庭）。这也是"删了文件不等于消失"的底层解释。

## ❓ 你的疑问与解答（弱项记录）

- （暂无）

## 📌 学习情况总结（Module 09）

- 全部 8 个知识点已完成（第 3 讲 dd：块级复制 / 毁盘风险 / 取证用途）
- 得分：**8.4 / 10**（扣分项：压缩旗标 z/j 1.0、dd 表述不具体 0.6，均已入错题本）
- 强项：归档 vs 压缩辨析、tar 三操作与 `-tvf` 偷看、gzip 原地压缩语义、dd 块级定位、毁盘风险意识、取证"只分析镜像"概念
- 弱项（已记录）：① 压缩旗标 z/j（已巩固）② tar `-f` 后紧跟归档名（Q4-1 复验通过）③ **表述笼统**——Q3 与 Q4 连犯（答"底层""不破坏证据"而非具体参数/机制），已列入跨模块待巩固
- **接续位置**：Module 09 已完成 → 下一模块 **Module 10（文件系统与存储设备）** 未开始